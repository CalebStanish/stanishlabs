# New Repository

This directory is a starter repository scaffold.

## Included
- `README.md` (this file)
- `.gitignore` for common OS/editor and dependency artifacts

## Initialize as its own Git repository

```bash
cd new-repository
git init
git add .
git commit -m "Initial commit"
```
